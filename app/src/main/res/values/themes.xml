<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.HsnBank" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar rengi -->
        <item name="android:statusBarColor">@color/hsnMavi</item>

        <!-- Status bar ikonlarının rengini ayarla (beyaz ikonlar için 'false') -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Diğer renk ayarları -->
        <item name="colorPrimary">@color/hsnMavi</item>
        <item name="colorPrimaryVariant">@color/hsnMavi</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>
</resources>

